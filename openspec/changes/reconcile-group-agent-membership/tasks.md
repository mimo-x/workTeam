## 1. 精确复现

- [x] 1.1 对照目标群 OpenIM 成员，并在可执行 Case 中捕获 `/v1/rooms` 的错误 Agent DTO。
- [x] 1.2 添加自有 Agent 成员整体替换的服务端和桌面失败 BDD。

## 2. 成员一致性

- [x] 2.1 保证房间响应引用的 Agent 合并进桌面 Agent 索引。
- [x] 2.2 保存后核对目标成员与同步结果，不一致时保留管理弹窗并显示错误。
- [x] 2.3 在成员编辑器中明确整体替换语义和成员差异。

## 3. 验收

- [x] 3.1 通过双用户 BDD 验证管理页、`@` 候选、后台成员和 OpenIM Outbox 一致。
- [x] 3.2 运行 Bug 门禁、相关测试、Lint、类型检查和构建。
