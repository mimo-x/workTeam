## Why

好友消息与当前用户消息都使用 `user` 发送者类型，导致好友消息错误显示在右侧，无法快速区分消息来源。

## What Changes

- 使用当前用户 ID 而不是发送者类型判断消息归属。
- 统一群聊、好友私聊和 Agent 私聊的左右布局。
- 增加当前用户、好友、Agent 与系统消息的对齐回归测试。

## Impact

仅修改桌面端 Team Chat 消息布局，不改变消息数据和路由逻辑。
