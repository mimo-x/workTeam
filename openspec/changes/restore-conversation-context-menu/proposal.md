## Why

消息列表条目缺少此前要求的右键操作，用户无法快速置顶常用会话或清理当前列表。

## What Changes

- 为所有会话条目接入 shadcn ContextMenu。
- 支持持久化置顶、取消置顶和从消息列表删除。
- 删除当前会话时切换到下一条可见会话；新消息到达后恢复已删除会话。
- 增加菜单渲染、排序和隐藏恢复回归测试。

## Impact

仅影响桌面端消息列表和本机展示偏好，不删除底层群组、Task 或远端消息数据。
