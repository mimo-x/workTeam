## 1. Implementation

- [x] 1.1 Recover stale device IDs by creating a new device and keep asynchronous WebSocket errors inside the handler; verify API remains alive
- [x] 1.2 Add backend regression cases for stale IDs and registration failures; verify protocol errors are actionable

## 2. Validation

- [x] 2.1 Run backend tests/build, desktop tests, lint, typecheck, and OpenSpec strict validation; verify all checks pass
