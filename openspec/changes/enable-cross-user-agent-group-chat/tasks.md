## 1. 复现与权限模型

- [x] 1.1 添加双用户跨成员 `@Agent` 的失败 BDD，并记录发布接口 404 基线。
- [x] 1.2 定义精确目标、单 Host 和非 Task 只读权限边界。

## 2. 跨成员聊天路由

- [x] 2.1 让 OpenIM 消息回调向目标 Agent 所有者的一个在线 Host 发布 `agent.chat.requested`。
- [x] 2.2 让桌面端只为事件中的自有本机 Agent 调用一次外部消息调度。
- [x] 2.3 保持本人提及、其他用户 Agent、Agent 消息和 Task 提议不发生重复或越权执行。

## 3. 双用户验证与部署

- [x] 3.1 注册两个临时用户并验证好友、Agent、群聊、消息镜像和回复 Outbox 完整链路。
- [x] 3.2 重建当前后台 API/Worker，确认 Agent 回复发布接口不再返回 404。

## 4. 验收

- [x] 4.1 回填 Bug 根因和验证结果，运行桌面/后台测试、Bug 门禁、Lint、类型检查和构建。
