## 1. 复现与修复

- [x] 1.1 添加后台同步群自有 Agent 提及 BDD，并确认修复前失败。
- [x] 1.2 在渲染层筛选自有本机 Agent，并将模型、精确目标和触发标记传给编排器。
- [x] 1.3 让外部消息的显式 Agent ID 成为权威调度范围，避免执行其他用户 Agent。
- [x] 1.4 增加鉴权且幂等的后台 Agent 回复发布接口，并按房间来源选择发布通道。

## 2. 验收

- [x] 2.1 运行相关回归、桌面全量测试、Bug 门禁、lint、typecheck 和 build。
- [ ] 2.2 在指定群中实际发送 `@Agent` 消息并确认回复回到原群。
